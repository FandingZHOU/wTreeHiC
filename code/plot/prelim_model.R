reg_data<-readRDS("for_regression_2.rds")
attach(reg_data)
boxplot(ave_week_temp)
boxplot(ave_week_rh)
boxplot(ave_week_vsby)
reg_data_filter<-na.omit(reg_data)

reg_data_filter<-reg_data_filter[reg_data_filter$acc_week_case>100,]
attach(reg_data_filter)
pairs(reg_data_filter[,c(5,13:15,17)])
boxplot(acc_week_case)
par(mfrow=c(1,2))
hist(case_per_km)
hist(log(case_per_km))
mod<-lm(case_per_km~ave_week_temp+ave_week_rh+ave_week_vsby+pop_density_2016_sq_km)
summary(mod)
par(mfrow=c(2,2))
plot(mod)
mod2<-lm(log(case_per_km)~ave_week_temp+ave_week_rh+ave_week_vsby+pop_density_2016_sq_km)
par(mfrow=c(2,2))
plot(mod2)
summary(mod2)
